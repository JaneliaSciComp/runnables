[project]
name = "runnables"
description = "JSON Schema for Fileglancer runnables.yaml manifests"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[tasks]
generate-schema = "python generate_schema.py"

[dependencies]
python = ">=3.12"
pydantic = ">=2.10,<3"
